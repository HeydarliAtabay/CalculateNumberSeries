import { Box, Typography } from "@mui/material";

export function MainPage() {
  return (
    <Box sx={{display:'flex', margin:'auto', textAlign:'center', width:'100%'}}>
      <Typography>This is main Page</Typography>
    </Box>
  );
}
